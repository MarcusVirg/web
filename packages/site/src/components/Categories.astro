---
type Props = {
	categories: string[]
}

const { categories } = Astro.props
---

<ol class="flex space-x-2">
	{
		categories.map((category) => (
			<li class="bg-gradient-to-br from-brand-blue via-indigo-500 to-brand-violet rounded-full p-0.5">
				<div class="bg-zinc-900 px-3 py-1 rounded-full">
					<span class="font-semibold text-transparent bg-clip-text bg-gradient-to-br from-brand-blue to-brand-violet">
						{category}
					</span>
				</div>
			</li>
		))
	}
</ol>
