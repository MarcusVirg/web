{
	"name": "web",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"workspaces": [
		"sites/*"
	],
	"scripts": {
		"archive:dev": "npm run --prefix sites/archive dev",
		"archive:build": "npm run --prefix sites/archive build",
		"archive:preview": "npm run --prefix sites/archive preview",
		"home:dev": "npm run --prefix sites/home dev",
		"home:build": "npm run --prefix sites/home build",
		"home:preview": "npm run --prefix sites/home preview"
	},
	"devDependencies": {
		"prettier": "^3.4.2"
	}
}
