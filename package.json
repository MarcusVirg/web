{
	"name": "web",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"workspaces": [
		"sites/*"
	],
	"scripts": {
		"archive:dev": "npm run --workspace sites/archive dev",
		"archive:build": "npm run --workspace sites/archive build",
		"archive:preview": "npm run --workspace sites/archive preview",
		"home:dev": "npm run --workspace sites/home dev",
		"home:build": "npm run --workspace sites/home build",
		"home:preview": "npm run --workspace sites/home preview"
	},
	"devDependencies": {
		"prettier": "^3.4.2",
		"typescript": "^5.7.2"
	},
	"overrides": {
		"esrap": "~1.2.0"
	}
}
