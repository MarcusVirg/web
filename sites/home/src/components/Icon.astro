---
// @ts-ignore
import { Icon } from 'astro-icon/components'

type Props = {
	class?: string
	name: string
}
const { class: className = '', name } = Astro.props
---

<Icon class={className} name={name} />
